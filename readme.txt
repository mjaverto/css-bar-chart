Simple HTML / CSS percentage based width horizontal bar chart.

After deciding the re-write the About section on my website, I looked around for some inspiration and realized many bar graphs around the net were just images. I decided to create some HTML / CSS that could create a nice looking bar graph without needing an image. 